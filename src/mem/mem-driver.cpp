#include <iostream>
#include "memory.h"

using namespace std;


int main() {
//    reg7 = 1231;
//    uint32_t address = 0;
//    write_to_memory(0,reg7);
//    cout << read_from_memory(0);
//    // print out clock cycles taken
//    // run with time (unix function)
//    return 0;


    int clock;
    // create memory objects
    while(file_of_mem_instr != EOF) {
        // parse current line
        // read or write to memory
        // take care of clock
        // initialize delay variables as needed
        // cout some stuff (i.e. showing we correctly parsed parts of the instructions and so on)
    }
}
